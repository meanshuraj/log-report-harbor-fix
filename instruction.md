Read the Apache-style access log at /app/access.log and create a JSON report at /app/report.json.

Success criteria:

1. The output file must exist at /app/report.json.
2. Include total_requests, the total number of log entries.
3. Include unique_ips, the number of distinct client IP addresses.
4. Include top_path, the most frequently requested path.

You have 120 seconds to complete the task.